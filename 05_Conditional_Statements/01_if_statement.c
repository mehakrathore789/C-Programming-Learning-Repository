/*
 * Program: if Statement
 * Description: Executes a block of code only if the condition is true.
 */

#include <stdio.h>

int main(void) {
    int marks = 85;

    if (marks >= 40) {
        printf("Result: Pass\n"); // Runs only because the condition is true
    }

    return 0;
}
